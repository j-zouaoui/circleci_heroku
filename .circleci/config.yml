# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1


orbs:

  python: circleci/python@1.5.0

# Define a job to be invoked later in a workflow.

jobs:
  build-and-test: #

    docker:
      - image: cimg/python:3.10.2
    # Checkout the code as the first step. This is a dedicated CircleCI step.

    steps:
      - checkout
      - run:
          name: install dependencies
          command: |
            pip install -r requirements.txt
      - run:
          name: Run tests
          # This assumes pytest is installed via the install-package step above
          command:       
<<<<<<< HEAD
            python3 manage.py test lettings.tests 
  
=======
            python3 manage.py test lettings.tests profiles.tests
       #create an image of the application
      - setup_remote_docker:
          version: 18.06.0-ce
>>>>>>> 981a448 (config file has been modefied to try to fix test problem)
      - run:
          name: Build Docker image
          command: |
            docker build -t heroku-dockerize:circleci .
            docker images

# Invoke jobs via workflows

workflows:
  sample: # This is the name of the workflow, feel free to change it to better match your workflow.
    # Inside the workflow, you define the jobs you want to run.
    jobs:
      - build-and-test
<<<<<<< HEAD

=======
>>>>>>> 981a448 (config file has been modefied to try to fix test problem)
